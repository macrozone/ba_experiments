const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SegmentationImage from '../segmentation_image';

describe('segmentation.components.segmentation_image', () => {
  it('should do something');
});
