const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import RegionSizeSlider from '../region_size_slider';

describe('segmentation.components.region_size_slider', () => {
  it('should do something');
});
