const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ToggleAxesButton from '../toggle_axes_button';

describe('threed_viewer.components.toggle_axes_button', () => {
  it('should do something');
});
