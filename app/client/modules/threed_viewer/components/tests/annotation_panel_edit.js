const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import AnnotationPanelEdit from '../annotation_panel_edit';

describe('threed_viewer.components.annotation_panel_edit', () => {
  it('should do something');
});
